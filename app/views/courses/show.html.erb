<br/>
<div class="box col-10 offset-1">
  <h1 class="text-center"><%= @course.title %></h1>
  <br/>
  <%= link_to 'Preview as a Student...', course_path(@course), class: 'btn btn-warning float-right' %>
  <br class = "clr" />

  <div class = "text-center">
    <%= link_to 'Add a new section...', new_instructor_course_section_path(@course), class: 'btn btn-primary' %>
  </div>

  <ul>
    <% @course.sections.each do |section| %>
      <li>
        <%= section.title %>
      </li>
    <% end %>
  </ul>

    
  <div class="row">
    <div class="col-sm-4 col-12 text-center">
      <h3><span class="badge badge-success">
        <%= number_to_currency @course.cost %>
      </span></h3>

      <% if current_user && current_user ==@course.user %>
        <br/>
       
        <%= link_to 'Administer', instructor_course_path(@course), class: 'btn btn-danger btn-large' %>
      <% end %>
    </div>

    <div class="col-sm-8 col-12">
      <%= @course.description %>
    </div>
  </div>
  <br/>
</div>
